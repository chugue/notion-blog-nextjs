---
phase: 01-build-recovery
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - infrastructure/queries/post.query.ts
  - app/api/notion-block-image/route.ts
  - app/api/notion/page/route.ts
  - app/(blog)/blog/[id]/page.tsx
autonomous: true
requirements: [BUILD-02]
must_haves:
  truths:
    - "All 3 unofficial-API consumers route through the normalized path (getNotionPage, or normalize-on-cache-return)"
    - "A cache HIT on stale double-wrapped data is still normalized before return"
    - "The image proxy route reads a defined block.type after normalization"
    - "The page route returns flat { role, value } records to clients"
    - "Every published post flows through generateStaticParams (SKIP_SSG_IDS is empty)"
  artifacts:
    - path: "infrastructure/queries/post.query.ts"
      provides: "normalize-on-cache-return"
      contains: "normalizeRecordMap"
    - path: "app/api/notion-block-image/route.ts"
      provides: "normalized image-proxy fetch"
      contains: "getNotionPage"
    - path: "app/api/notion/page/route.ts"
      provides: "normalized page fetch"
      contains: "getNotionPage"
    - path: "app/(blog)/blog/[id]/page.tsx"
      provides: "emptied SKIP_SSG_IDS"
      contains: "SKIP_SSG_IDS"
  key_links:
    - from: "infrastructure/queries/post.query.ts"
      to: "infrastructure/database/external-api/normalize-record-map.ts"
      via: "normalize on cachedFn() result"
      pattern: "normalizeRecordMap"
    - from: "app/api/notion-block-image/route.ts"
      to: "infrastructure/database/external-api/notion-client.ts"
      via: "getNotionPage"
      pattern: "getNotionPage"
    - from: "app/api/notion/page/route.ts"
      to: "infrastructure/database/external-api/notion-client.ts"
      via: "getNotionPage"
      pattern: "getNotionPage"
---

<objective>
Wire every unofficial-API consumer to the normalized path and empty the SKIP_SSG_IDS set so
all posts flow through static generation.

Purpose: Plan 01 built the normalizer + getNotionPage entry point. This plan routes the 3 current
`notionAPI.getPage` consumers through normalized data (BUILD-02) and removes the per-post build
workaround (D-06, ROADMAP criterion #2). The cache path uses normalize-on-return so stale
double-wrapped cache hits are also fixed (D-04). No cache purge / revalidateTag is added (D-05).

Output: post.query.ts normalizes the cache return; both API routes call getNotionPage;
SKIP_SSG_IDS is emptied (constant + filter left structurally in place for Phase 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-recovery/01-CONTEXT.md
@.planning/phases/01-build-recovery/01-PATTERNS.md
@docs/빌드실패-replaceAll-진단-보고서.md
@.planning/phases/01-build-recovery/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Normalize the cache return in post.query.ts</name>
  <files>infrastructure/queries/post.query.ts</files>
  <read_first>
    - infrastructure/queries/post.query.ts (lines ~48-80) — getPostByIdQuery, the unstable_cache wrapper and the success return (the file being modified)
    - .planning/phases/01-build-recovery/01-CONTEXT.md <decisions> D-04 (normalize the cachedFn() result, not inside the cached fn) and D-05 (no revalidateTag / key-bumping)
    - .planning/phases/01-build-recovery/01-PATTERNS.md "post.query.ts (MODIFY)" section — exact change point and import path
    - docs/빌드실패-replaceAll-진단-보고서.md §4 "캐시 정합성" — why normalize must be on the return value
  </read_first>
  <action>
    Apply `normalizeRecordMap()` to the RETURN VALUE of the cache wrapper (the `result` of `await cachedFn()`), AFTER the existing `if (!result)` guard, at the success return — e.g. wrap the existing `data:` expression so it becomes `data: normalizeRecordMap(result as unknown as notionType.ExtendedRecordMap)`. This is D-04: a cache HIT on old double-wrapped `.next/cache` data must also be normalized; idempotency makes the double-apply harmless (report §4).
    Do NOT change the cached function body — leave it calling `notionAPI.getPage(id)`. Do NOT switch the cached fn to `getNotionPage` (D-04 deliberately normalizes the return value so stale hits are covered).
    Add `import { normalizeRecordMap } from '../database/external-api/normalize-record-map';` (relative, matching the existing `'../database/external-api/notion-client'` import on line 6).
    Do NOT change the `unstable_cache` keys (`post-${id}`) or tags (`post-${id}`, `all-posts`). Do NOT add `revalidateTag` or key-bumping (D-05). Keep the existing `Result<T>` envelope — this query boundary is where Result IS expected.
  </action>
  <verify>
    <automated>grep -q "normalizeRecordMap(result" infrastructure/queries/post.query.ts && grep -q "from '../database/external-api/normalize-record-map'" infrastructure/queries/post.query.ts && ! grep -q "revalidateTag" infrastructure/queries/post.query.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `normalizeRecordMap` is applied to `result` (the `await cachedFn()` value), AFTER the `!result` guard, at the success return.
    - The cached function body still calls `notionAPI.getPage(id)` (NOT switched to getNotionPage).
    - `unstable_cache` keys/tags are unchanged; no `revalidateTag` / key-bump added (`grep -c revalidateTag` returns 0).
    - The `Result<T>` envelope return shape is preserved.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>post.query.ts normalizes the cache return value (D-04), keeps cache keys/tags and Result envelope intact, adds no revalidation (D-05), and typechecks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route both API routes through getNotionPage</name>
  <files>app/api/notion-block-image/route.ts, app/api/notion/page/route.ts</files>
  <read_first>
    - app/api/notion-block-image/route.ts (lines 1, ~20, ~37-39, error branches) — the file being modified; reads block.type/block.format
    - app/api/notion/page/route.ts (lines 1, 4, ~26, Result envelope + CORS branches) — the file being modified
    - .planning/phases/01-build-recovery/01-CONTEXT.md <decisions> D-03, D-07 — single entry point; spot-check shapes
    - .planning/phases/01-build-recovery/01-PATTERNS.md "notion-block-image/route.ts" and "notion/page/route.ts" sections — single-line swaps + indent notes (image route 4-space, page route 2-space)
  </read_first>
  <action>
    In `app/api/notion-block-image/route.ts`: change the import to bring in `getNotionPage` from `@/infrastructure/database/external-api/notion-client` (absolute alias — App-router files use `@/...`), and replace `await notionAPI.getPage(blockId)` with `await getNotionPage(blockId)`. After normalization `recordMap.block[blockId]?.value` is the flat block, so `block.type === 'image'` and `block.format` resolve correctly (D-07, ROADMAP criterion #3). Leave the `if (!block)` 404 guard and the try/catch → 500 branch untouched. Keep 4-space indent.
    In `app/api/notion/page/route.ts`: import `getNotionPage` from the same `@/infrastructure/database/external-api/notion-client` module and replace `await notionAPI.getPage(pageId)` with `await getNotionPage(pageId)`. The client now receives flat `{ role, value }` records (D-07, ROADMAP criterion #4). Keep the `Result`-envelope return shape, CORS headers, and `!result` 404 / catch 500 branches untouched. Keep 2-space indent.
    These are single-line swaps each — do NOT change error handling, response shapes, or remove the `notionAPI` import if other symbols from it are still used (verify by reading the file; in both routes `getPage` was the only usage, so the import can switch from `notionAPI` to `getNotionPage`).
  </action>
  <verify>
    <automated>grep -q "getNotionPage(blockId)" app/api/notion-block-image/route.ts && grep -q "getNotionPage(pageId)" app/api/notion/page/route.ts && [ "$(grep -rc 'notionAPI.getPage' app/api/)" = "app/api/notion-block-image/route.ts:0
app/api/notion/page/route.ts:0" ] 2>/dev/null; grep -rL 'notionAPI.getPage' app/api/notion-block-image/route.ts app/api/notion/page/route.ts | wc -l | grep -q 2 && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/notion-block-image/route.ts` calls `getNotionPage(blockId)` and no longer calls `notionAPI.getPage`.
    - `app/api/notion/page/route.ts` calls `getNotionPage(pageId)` and no longer calls `notionAPI.getPage`.
    - `grep -rc 'notionAPI.getPage' app/api/` reports 0 in both route files.
    - Both routes import `getNotionPage` from `@/infrastructure/database/external-api/notion-client`.
    - Error handling, CORS, 404/500 branches, and Result envelope shapes are unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Both API routes fetch via getNotionPage (BUILD-02, D-03); no raw notionAPI.getPage remains in app/api/; shapes/error handling preserved; typechecks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Empty the SKIP_SSG_IDS set</name>
  <files>app/(blog)/blog/[id]/page.tsx</files>
  <read_first>
    - app/(blog)/blog/[id]/page.tsx (lines ~20-33) — SKIP_SSG_IDS Set and generateStaticParams filter (the file being modified)
    - .planning/phases/01-build-recovery/01-CONTEXT.md <decisions> D-06 — empty the set in Phase 1; leave constant + filter structurally in place (deleting them is Phase 3 / RENDER-01)
    - .planning/phases/01-build-recovery/01-PATTERNS.md "blog/[id]/page.tsx (MODIFY)" section — empty the set, keep filter + JSDoc, 4-space indent
  </read_first>
  <action>
    Empty the `SKIP_SSG_IDS` set: change `new Set([...two IDs...])` to an empty set — `const SKIP_SSG_IDS = new Set<string>();` (D-06). Remove the two hardcoded IDs (`2c39c76c-6cb4-80f0-a79e-e935e2bed857`, `2c59c76c-6cb4-803e-95eb-f0fe5d659685`).
    LEAVE the constant declaration, its JSDoc comment, and the `.filter((post) => !SKIP_SSG_IDS.has(post.id))` call in `generateStaticParams` structurally in place — deleting the dead constant/filter is Phase 3 (RENDER-01), NOT Phase 1. This keeps the 1↔3 boundary clean: Phase 1 = functionally unneeded, Phase 3 = code removed + render audit. Every published post now flows through generateStaticParams (ROADMAP criterion #2).
    Do NOT change generateStaticParams logic otherwise. Keep 4-space indent.
  </action>
  <verify>
    <automated>grep -q "SKIP_SSG_IDS" app/\(blog\)/blog/\[id\]/page.tsx && ! grep -q "2c39c76c-6cb4-80f0-a79e-e935e2bed857" app/\(blog\)/blog/\[id\]/page.tsx && ! grep -q "2c59c76c-6cb4-803e-95eb-f0fe5d659685" app/\(blog\)/blog/\[id\]/page.tsx && grep -q "SKIP_SSG_IDS.has" app/\(blog\)/blog/\[id\]/page.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `SKIP_SSG_IDS` is now an empty set; neither hardcoded ID string remains in the file.
    - The `SKIP_SSG_IDS` constant declaration AND the `.filter(...SKIP_SSG_IDS.has...)` call are still present (NOT deleted — Phase 3 owns deletion).
    - `generateStaticParams` logic is otherwise unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>SKIP_SSG_IDS is empty (D-06); constant + filter remain structurally; every published post flows through generateStaticParams; typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion unofficial API → app | Untrusted upstream recordMap, now routed through the normalizer at all 3 consumers |
| App server → client (/api/notion/page) | Normalized recordMap returned to the browser; must not leak raw upstream wrapper shape |
| App server → client (/api/notion-block-image) | Block metadata read server-side to resolve fresh image URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | /api/notion/page returning raw double-wrapped upstream shape | mitigate | Route now calls getNotionPage → returns flat `{ role, value }` records only; no raw wrapper leaks (D-07 criterion #4) |
| T-01-04 | Denial of Service | Stale double-wrapped cache hit crashing render | mitigate | normalize-on-return in post.query.ts (D-04) covers cache HITs; idempotent so safe |
| T-01-05 | Denial of Service | Image route reading undefined block.type | mitigate | getNotionPage normalization makes block.type defined (D-07 criterion #3); existing `if (!block)` 404 guard retained |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — only existing modules imported; no legitimacy gate needed |

No HIGH-severity threats: changes are internal data-shape fixes and a build-config (SKIP_SSG_IDS) edit; no new external input, no new dependencies, no auth surface.
</threat_model>

<verification>
- `grep -rc 'notionAPI.getPage' app/api/` reports 0 in both route files (only normalizer/client own the raw call indirectly via getNotionPage).
- `grep -c 'notionAPI.getPage' infrastructure/queries/post.query.ts` reports 1 (cached fn intentionally keeps the raw call per D-04).
- SKIP_SSG_IDS is empty; neither hardcoded ID remains.
- `npx tsc --noEmit` passes across all four edits.
</verification>

<success_criteria>
- All 3 consumers use normalized data: post.query via normalize-on-return (D-04), both routes via getNotionPage (D-03) — BUILD-02.
- No cache purge/revalidateTag added (D-05).
- SKIP_SSG_IDS emptied, constant + filter retained for Phase 3 (D-06).
- TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/phases/01-build-recovery/01-02-SUMMARY.md` when done.
</output>
