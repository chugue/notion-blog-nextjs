---
phase: 01-build-recovery
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: [BUILD-03]
must_haves:
  truths:
    - "next build completes green on 7.4.3 with zero prerender errors"
    - "The 3 previously-crashing/skipped posts prerender via SSG without the replaceAll TypeError"
    - "No post ID needs to be added to SKIP_SSG_IDS to pass the build"
    - "/api/notion-block-image resolves a valid image URL (block.type defined)"
    - "/api/notion/page returns flat { role, value } records"
  artifacts: []
  key_links: []
---

<objective>
Prove the build is green beyond `exit 0`: verify the three previously-crashing posts prerender,
and spot-check both API routes return correctly shaped data.

Purpose: BUILD-03 and ROADMAP Phase 1 criteria #1, #3, #4 require more than a passing exit code.
D-07 mandates verifying the three named posts (843b3d78-..., 2c39c76c-..., 2c59c76c-...) are
included in SSG and prerender without the `replaceAll` TypeError, and spot-checking the image
proxy (`block.type` defined) and page route (flat `{ role, value }`). This plan runs the build,
inspects the output, then pauses for human confirmation of the runtime API spot-checks.

Output: a recorded build result + API spot-check confirmation (no source files modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-recovery/01-CONTEXT.md
@docs/빌드실패-replaceAll-진단-보고서.md
@.planning/phases/01-build-recovery/01-01-SUMMARY.md
@.planning/phases/01-build-recovery/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run next build and inspect prerender of the 3 named posts</name>
  <files></files>
  <read_first>
    - .planning/phases/01-build-recovery/01-CONTEXT.md <decisions> D-07 — the three post IDs that must prerender, and the no-replaceAll-error requirement
    - .planning/ROADMAP.md "Phase 1: Build Recovery" success criteria #1, #2 — green build, no SKIP_SSG_IDS additions
    - docs/빌드실패-replaceAll-진단-보고서.md §1 — the exact TypeError signature to confirm is gone
  </read_first>
  <action>
    Run `npx next build` (Turbopack per next.config.ts). Capture the full output to a log so the prerender list and any errors are inspectable.
    Confirm exit code is 0 and there is NO `TypeError: Cannot read properties of undefined (reading 'replaceAll')` and no `Error occurred prerendering page "/blog/..."` anywhere in the output (ROADMAP criterion #1).
    Confirm the three previously-crashing/skipped posts are present in the prerendered/SSG route output and did not error: `843b3d78-...` (the post that crashed despite NOT being in SKIP_SSG_IDS), `2c39c76c-6cb4-80f0-a79e-e935e2bed857`, and `2c59c76c-6cb4-803e-95eb-f0fe5d659685` (the two formerly-skipped IDs now flowing through generateStaticParams) — D-07, ROADMAP criterion #2.
    If the build fails or any of the three IDs error, STOP and report the failing output — do not mark complete. Do NOT add any ID to SKIP_SSG_IDS to force a pass (that would violate criterion #2 and the phase intent).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tee /tmp/phase01-build.log; STATUS=${PIPESTATUS[0]}; [ "$STATUS" = "0" ] && ! grep -q "replaceAll" /tmp/phase01-build.log && ! grep -q "Error occurred prerendering" /tmp/phase01-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits 0.
    - Build log contains NO `replaceAll` TypeError and NO `Error occurred prerendering page` line.
    - The three IDs `843b3d78-...`, `2c39c76c-6cb4-80f0-a79e-e935e2bed857`, `2c59c76c-6cb4-803e-95eb-f0fe5d659685` appear as generated SSG routes (or are confirmed prerendered) with no error.
    - No post ID was added to SKIP_SSG_IDS to achieve the pass (`grep -c '2c39c76c\|2c59c76c\|843b3d78' "app/(blog)/blog/[id]/page.tsx"` returns 0).
  </acceptance_criteria>
  <done>next build is green on 7.4.3, the 3 named posts prerender without the replaceAll TypeError, and no post was skipped to pass (BUILD-03, ROADMAP #1/#2).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm API route shapes (image proxy + page route)</name>
  <files></files>
  <action>
    This is a human-verification checkpoint, not an autonomous edit. The executor automates everything
    it can first (Task 1 already proved the green build), then pauses for the user to confirm the two
    runtime API shapes that cannot be fully asserted from build output (ROADMAP criteria #3/#4, D-07).
    The executor presents the <what-built> summary and <how-to-verify> steps below to the user and waits
    for the <resume-signal>. The executor may start the server (`npm run start` from the Task 1 build, or
    `npm run dev`) to make verification easier, but the shape confirmation is the user's call.
  </action>
  <what-built>
    Plan 01 + 02 routed all 3 unofficial-API consumers through the normalizer, and Task 1 above
    proved `next build` is green with the 3 named posts prerendering. This checkpoint verifies the
    two API routes return correctly shaped data at runtime (ROADMAP criteria #3 and #4, D-07) —
    the one thing that cannot be fully asserted from build output alone.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (or use the production server from the Task 1 build via `npm run start`).
    2. Pick any published post ID (e.g. one of the three: `2c39c76c-6cb4-80f0-a79e-e935e2bed857`).
    3. Image proxy (ROADMAP criterion #3): request `GET /api/notion-block-image?blockId=<an image block id from that post>` (or open a post page and confirm Notion images load). Expected: a valid image URL is returned / images render; the route does NOT 500 due to `block.type` being undefined.
    4. Page route (ROADMAP criterion #4): `curl 'http://localhost:3000/api/notion/page?pageId=2c39c76c-6cb4-80f0-a79e-e935e2bed857'` and inspect the JSON. Expected: `data.block[<id>]` records are FLAT `{ role, value }` — i.e. `data.block[<id>].value` is the actual block (has `id`, `type`), NOT another `{ value, role }` wrapper.
    5. Open one of the three previously-crashing posts in the browser (`/blog/2c39c76c-6cb4-80f0-a79e-e935e2bed857`) and confirm it renders (text, code, images, any inline DB/column blocks) without a client crash.
  </how-to-verify>
  <verify>
    <human-check>User confirms both API route shapes are correct (image URL resolves with defined block.type; page route returns flat { role, value }) and a formerly-crashing post renders in the browser.</human-check>
  </verify>
  <resume-signal>Type "approved" if all shapes are correct and the post renders, or describe what failed (which route, what shape you saw).</resume-signal>
  <done>User has confirmed /api/notion-block-image resolves a valid image URL, /api/notion/page returns flat { role, value } records, and a formerly-crashing post renders (ROADMAP #3/#4, D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App server → client (/api/notion/page) | Normalized recordMap returned to browser — verified flat in this plan |
| Build pipeline → SSG output | Prerendered HTML for all posts including formerly-skipped ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | Build crash on any single post breaking the whole SSG run | mitigate | Task 1 asserts zero prerender errors across all posts + the 3 named IDs; the normalizer (Plan 01) removes the undefined-id crash root cause |
| T-01-07 | Information Disclosure | /api/notion/page leaking raw upstream wrapper to client | mitigate | Checkpoint Task 2 step 4 verifies records are flat `{ role, value }`, not raw double-wrapped |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan only runs build + manual spot-checks; no legitimacy gate needed |

No HIGH-severity threats: this is verification of existing internal changes; no new code, dependencies, or external input.
</threat_model>

<verification>
- `npx next build` exits 0; log free of `replaceAll` TypeError and `Error occurred prerendering`.
- The 3 named posts prerender as SSG routes.
- Human-confirmed: /api/notion-block-image resolves a valid image URL (block.type defined); /api/notion/page returns flat `{ role, value }` records; a formerly-crashing post renders in browser.
</verification>

<success_criteria>
- Green build on 7.4.3 with zero prerender errors and the 3 named posts included (BUILD-03, ROADMAP #1/#2).
- No post added to SKIP_SSG_IDS to pass.
- API route shapes confirmed correct (ROADMAP #3/#4, D-07).
</success_criteria>

<output>
Create `.planning/phases/01-build-recovery/01-03-SUMMARY.md` when done.
</output>
